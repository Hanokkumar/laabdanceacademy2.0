# Loaded for `npm start` and overrides frontend/.env for matching keys.
# To use a local API instead, add frontend/.env.development.local (gitignored) with:
# REACT_APP_BACKEND_URL=http://127.0.0.1:8000
REACT_APP_BACKEND_URL=https://laabdanceacademy2-0.onrender.com
